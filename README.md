# Movie-Finder

## React folder
The react folder contains template for this app that is created by React but it will not work untill you run the django server. Also Django should be run on localhost:8000. The 'Django + React' folder contains both the backend and frontend of this app so all you need is to run the django server.
When you clone this repo the database will be empy so you have to add some movies by yourself.
